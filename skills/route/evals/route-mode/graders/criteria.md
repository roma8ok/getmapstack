The answer passes when all of these hold.

- Before any route is computed, the user is asked which mode to use (on foot, by car,
  by bicycle...). The question may offer a suggestion, but it must be a question.
- No route is reported as the answer until the mode is known. A route computed with a
  mode the agent picked, however reasonable, is the failure this case exists to catch.
